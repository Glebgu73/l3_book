<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :publishing_house %><br />
    <%= f.text_field :publishing_house %>
  </div>
  <div class="field">
    <%= f.label :production_year %><br />
    <%= f.number_field :production_year %>
  </div>
  <div class="field">
    <%= f.label :number_of_pages %><br />
    <%= f.number_field :number_of_pages %>
  </div>
	<% unless @book.new_record? %>
	    <%= link_to 'download', book_change_state_path(@book, book: :download), method: :put %> |
	    <%= link_to 'delete', book_change_state_path(@book, book: :del), method: :put %> 
	  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
